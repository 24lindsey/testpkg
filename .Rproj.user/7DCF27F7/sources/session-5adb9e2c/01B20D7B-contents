#' Say Hello
#'
#' Prints a greeting.
#' @export
hello <- function() {
  print("Hello, world!")
}
max_na <- function(x) ifelse( !all(is.na(x)), max(x, na.rm=T), NA)
min_na <- function(x) ifelse( !all(is.na(x)), min(x, na.rm=T), NA)
